---
import { cn } from '../../../lib/utils'
const { class: className, ...rest }: any = Astro.props;
---
<div
   
    class={cn(
      "rounded-lg border bg-card text-card-foreground shadow-xs",
      className
    )}
    {...rest}
  ><slot /></div>
