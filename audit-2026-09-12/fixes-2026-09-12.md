# Fixes Shipped — 2026-09-12

Follow-up to `audit-full-2026-09-12.md`. Built and verified in `dist/`.

## What was fixed in this repo

| # | Finding | Fix | Verified by |
|---|---|---|---|
| 1 | Hero CTAs were dead buttons | `Button.astro` now renders `<a>` when `href` is supplied and `<button type="...">` otherwise; **15 buttons** previously without an `href` now go to real targets | Built HTML: 0 dead homepage buttons; both hero CTAs are `<a href="/consultation/">` and `<a href="/portfolio/">` |
| 2 | All 11 dead navigation CTAs across the site | Each `<Button>` now carries the correct `href` | Grep on `dist/`: 0 remaining unwrapped, non-submit Buttons without `href` |
| 3 | 20 placeholder `href: "#"` CTAs across 5 service pages | All 20 now point at real targets (`/consultation/` for consult-intent; `/portfolio/` for "see samples") | Grep on `dist/services/**`: 0 `href="#"` remain |
| 4 | Consult-intent CTAs pointed to `/contact/` while a dedicated `/consultation/` page existed (mismatch) | Header (desktop + mobile), `ServicesSection`, `CTASection` now route to `/consultation/` | Built HTML inspection |
| 5 | Mobile hero overlapped the fixed header | `HeroSection.astro`: `min-h-screen` → `min-h-[100svh]` with `pt-28 pb-16 sm:pt-32`; stray `animate-bounce` particle dots removed | Live CDP measurement at 390×844: header bottom 82 px, H1 top 184 px → **102 px clearance** (was overlapping). Screenshot: `audit-2026-09-12/shots-after/home-mobile-fixed.png` |
| 6 | Homepage H1 was a generic greeting, carried no keyword | New H1: **«خدمات هوش مصنوعی و سئو حرفه‌ای در ایران»** with gradient on the key phrase | `dist/index.html` H1 text |
| 7 | Invalid HTML: 2,518 duplicate `class` attributes (lucide SVG injection bug) | `Icon.astro`: now merges our class into the icon's existing `class` attribute instead of emitting a second one | Built HTML: **0 duplicate `class`** across all 45 pages |
| 8 | No `prefers-reduced-motion` despite 11 keyframe animations | `globals.css`: media query disables animations for users who request reduced motion | CSS contains `prefers-reduced-motion: reduce` |
| 9 | No global `:focus-visible` ring (only the Button component had it) | `globals.css`: zero-specificity `:where()` ring for keyboard users | CSS contains `:focus-visible` rule |
| 10 | No `scroll-padding-top` for the fixed header → in-page anchors landed under it | `globals.css`: `html { scroll-padding-top: 6rem }` | CSS contains the rule |
| 11 | Consultation and contact form labels had no `for`/`id` association | Added `for`/`id` pairs to all 5 fields on both pages, plus `autocomplete` (name / email / tel) | Built HTML: 5 labels with `for=`, 5 controls with `id=` on each page |
| 12 | Zero `hreflang` sitewide (single-language site missed disambiguation) | `BaseLayout.astro` emits `<link rel="alternate" hreflang="fa-IR">` and `x-default` on every page | Built HTML: 45/45 pages with `hreflang="fa-IR"` |
| 13 | `_redirects` used `301!` forced syntax which the deployment silently ignored (`/home` and `/blog/post/*` returned 404) | Plain `301` everywhere with comment explaining the finding | Verified live: plain `301` rules resolved; `/home` etc. will resolve after deploy |
| 14 | Portfolio emitted only `ItemList` | `portfolioSchema` now uses `@graph` with a `CollectionPage` wrapper + `WebSite` `isPartOf` reference | `dist/portfolio/index.html` contains `CollectionPage` and `ItemList` |
| 15 | `/privacy/` and `/terms/` had no schema | Added `WebPage` schema + `BreadcrumbList` to both pages | Built HTML now: `WebPage` present, schema coverage **44/45** (only `/404/` correctly bare) |
| 16 | `_headers` HTML cache rule (`/*.html`) didn't match Cloudflare Pages' extensionless directory URLs, so HTML was served with `max-age=0, must-revalidate` | Moved `Cache-Control: public, max-age=3600` into the `/*` block; per-asset rules (which come later in the file) still override for hashed/`.webp`/`.woff2`/etc. | New `_headers` applies max-age=3600 to HTML pages; verify after deploy that webp still returns immutable |
| 17 | Contact form used the Resend API and leaked internal errors | `functions/api/send-email.ts` rewritten for **Cloudflare's native `send_email` binding**: origin allowlist, per-field length caps, email + Iranian mobile regex validation, HTML-escaped email body, `503` (not `500`) when the binding is absent, and generic client errors with details logged server-side only | `npx tsc --noEmit` clean; `npm run type-check` 0 errors |
| 18 | Unsourced `AggregateRating` (`4.9` / `127` reviews) + visible «۴.۹ از ۵ امتیاز» | Schema node removed with an inline comment explaining it may only return alongside real on-page reviews; hero claim replaced with a provable one («مشاوره رایگان پیش از عقد قرارداد») | `dist/`: 0 occurrences of `AggregateRating`, `ratingValue`, or `۴.۹ از ۵`. Only remaining `۴.۹` on the site is a price («۴.۹ میلیون تومان») |
| 19 | 5 of 7 blog posts were stale (2024 dates, no honest basis to bump `dateModified`) | Each got a **real** «به‌روزرسانی شهریور ۱۴۰۵» section written from 2026 sources, *then* `updated: "2026-09-12"`; `content-calendar-guide` also gained a missing «اشتباهات رایج» section | All 7 posts: `dateModified` 2026-09-11/12 in `dist/` and in `sitemap-0.xml` |
| 20 | `digital-marketing-trends-2024` framed everything as «۲۰۲۴» (also «ویدیو پادشاه محتوا در ۲۰۲۴», «فرمت‌های محبوب ۲۰۲۴») | Year references updated to ۲۰۲۶ / evergreen, plus a retrospective section on which trends actually survived | Grep on source: no stale «۲۰۲۴» framing remains |
| 21 | **51 in-article TOC anchors across all 7 posts were dead** — hand-written anchors didn't match Astro's generated heading IDs (Persian hamza/ZWNJ normalisation) | All TOC anchors regenerated against the real generated IDs; 4 TOC entries pointed at sections that didn't exist, so the missing «اشتباهات رایج در اجرای تقویم محتوایی» section was written; 2 orphan entries in `seo-faq-guide` removed | Script check: **51/51 anchors resolve** against `<h2>/<h3>` IDs in `dist/` |
| 22 | `src/lib/blog-data.ts` (legacy Next.js-era override table) silently shadowed MDX frontmatter and had **drifted out of sync** — different authors (سارا کریمی vs سارا احمدی, علی احمدی vs احمد محمدی) and different published dates | File deleted; `[slug].astro` and `blog/index.astro` now read the content collection directly, so MDX frontmatter is the single source of truth | All 7 posts: published dates and bylines now match their `.mdx`; all 7 still listed on `/blog/` |
| 23 | Sitemap emitted no `<lastmod>` at all, so refreshes were invisible to crawlers | `astro.config.mjs` parses `updated` (falling back to `date`) from each MDX file and injects `lastmod` via the sitemap `serialize` hook | `dist/sitemap-0.xml`: all 7 blog URLs carry `<lastmod>` |
| 24 | Three blog URLs still advertised their age (`seo-guide-2024`, `google-ads-guide-2024`, `digital-marketing-trends-2024`) even though the content is now current | Renamed to evergreen slugs (`seo-guide`, `google-ads-guide`, `digital-marketing-trends`) **before the Astro site goes live**, so there is no migration cost; 6 × `301` added to `_redirects` (both slash variants, since the legacy Next build served them with a trailing slash); `public/llms.txt` URLs updated | All 3 new URLs built with correct canonicals; **0 references to any `-2024` blog URL remain anywhere in `dist/`**; old slugs no longer emitted |
| 25 | **The homepage rendered zero images** (no `<img>`, no `<picture>`) — audit P1 #10 | New `PortfolioPreview.astro` section using the 5 **real** project images that already existed in `src/assets/images/`, each deep-linking to its case anchor on `/portfolio/`. Added to the homepage between `AllServicesSection` and `WhyChooseUsSection` | `dist/index.html`: **5 `<img>`** (avif + webp via the `Picture` component, responsive `srcset`), 5 `<picture>`, **0 images missing `alt`**, all 5 `/portfolio/#case-…` links resolve |
| 26 | Internal-link integrity had never been checked | Crawled every `href`/`src` in `dist/` against the built URL set, then separately validated every **fragment** target (my first pass had stripped `#…`) | **2,582 internal links, 0 broken**; **223 same-page + 15 cross-page anchors, 0 broken** — so the `/faq/` deep links the audit flagged are fine |
| 27 | Accordion exposed no state to assistive tech — `AccordionTrigger.astro` was a bare `<button>` with no `aria-expanded`, and the panel had no `role`/`aria-labelledby` | Static `aria-expanded="false"` on every trigger (accurate: all panels render hidden unless `defaultOpen`); the `Accordion.astro` script now wires `aria-controls`, `role="region"`, `aria-labelledby` with page-unique ids, and flips `aria-expanded` on **every** item when one opens (previously only the clicked item changed) | `dist/faq/`: 26/26 triggers carry `aria-expanded`; `dist/blog/ai-seo-guide/`: 6/6. Bundle contains the full wiring |
| 28 | Mobile menu could only be closed by tapping a link — no Escape key, focus was never returned to the trigger, and `aria-controls` pointed at a nonexistent id | Added `id="mobile-menu-panel"` + `aria-controls` on the trigger, `aria-expanded` in the initial markup, **Escape closes and restores focus** to the opener, and **Tab is trapped inside the open panel** | `dist/index.html`: `aria-controls="mobile-menu-panel"` → id exists; **0 `aria-controls` targets missing** sitewide; **0 duplicate ids** on any page; every page has exactly **1 `<h1>`** |
| 29 | No `HowTo` on `/services/content/content-calendar/` despite it being a step-based process | Added `HowTo` schema generated from the **same `workflowSteps` array that renders the visible numbered section**, soschema and markup cannot drift; each step's `url` points at a real `#step-N` anchor (ids added, with `scroll-mt-24` so they don't land under the fixed header) | `dist/…/content-calendar/index.html`: `HowTo` + 6 `HowToStep`, all 6 anchors present |
| 30 | `robots.txt` declared **two** sitemaps, one of which (`/sitemap.xml`) only exists via a 301 — declaring a redirecting sitemap URL is sloppy | Kept only the canonical `sitemap-index.xml` with a comment noting the legacy path still resolves via `_redirects` | Pre-deploy config audit: all URLs in `robots.txt` (2), `llms.txt` (44) and `llms-full.txt` (11) resolve; all 44 sitemap URLs resolve |
| 31 | **Every blog post rendered three blocks twice** — `BlogPost.astro` rendered a TOC accordion, a key-takeaways card and an FAQ accordion, **and then `<Content />` rendered `## نکات کلیدی` / `## فهرست مطالب` / `## سوالات متداول` again** from the MDX | Migrated all three blocks out of the MDX bodies into frontmatter (`keyTakeaways:` / `faqs:`), added them to the collection schema, and pointed `[slug].astro` at `entry.data`. The hand-written static TOC was dropped (the auto-generated one is better). First attempt — a remark plugin — **did not work**: `mdx({ remarkPlugins })` has no effect on content-collection MDX in this Astro version (verified with a clean rebuild after clearing `node_modules/.astro` and `node_modules/.vite`); adding `markdown.remarkPlugins` broke the config. Reverted and used the frontmatter approach instead | Per post: «نکات کلیدی» **2 → 1**, «سوالات متداول» **4 → 2**, body `h2`s no longer contain any of the three. Card still shows 4 takeaways, FAQPage schema still has 5–7 questions. `src/lib/blog.ts` (now unused) deleted. Build 45/45, 2,579 links 0 broken |
| 32 | Mobile menu: Escape did **not** restore focus to the trigger (the handler captured `document.activeElement`, which is `<body>` on iOS taps and on programmatic clicks, so focus was stranded). Also no way to dismiss the menu except following a link | `setMenu()` now takes the trigger element explicitly and falls back to the menu button if the remembered element is gone or is `<body>`; added click-outside dismissal with a `restoreFocus` flag so it doesn't steal focus (and scroll) right before navigation | CDP: `menu: Escape restores focus to trigger` **PASS**, `menu: outside click closes` **PASS**, `menu: Tab wraps inside panel` **PASS** |
| 33 | `/consultation/` threw away the server's validation message — `ajax-form.js` only checked `res.ok` and always showed the generic «خطایی رخ داد…» | The shared handler now parses the JSON response and shows the endpoint's Persian `error` (e.g. «شماره موبایل معتبر نیست (مثال: 09123456789)»), falling back to the generic message only for network failures | `node --check` clean; behaviour matches `/contact/`, which already surfaced `result.error` |
| 34 | `public/sw.js` was shipped but referenced by **no** page — a leftover from the Next.js era; `BaseLayout` actively unregisters all service workers | Deleted (source + `dist`) | `dist/sw.js` no longer emitted; 2,563 internal links and 180 anchors re-verified with **0** broken |
| 35 | `llms.txt` / `llms-full.txt` were hand-written in `public/` and drifted silently — the `-2024` slug renames earlier in this session are exactly the class of drift they invite | Replaced with `src/lib/llms.ts` (single source of truth: site taxonomy + service groups + company FAQs + portfolio case studies) and two Astro endpoints `src/pages/llms.txt.ts` and `src/pages/llms-full.txt.ts`. Blog posts are pulled from the content collection at build time so adding/renaming a post updates the file automatically; **every** hard-coded URL is validated against the real page routes via `assertPathsExist`, so a renamed service fails the build instead of shipping a dead link to Perplexity / ChatGPT / Gemini | `llms.txt`: 44 unique URLs, 0 broken; `llms-full.txt`: 33 unique URLs (was 12), 0 broken. The drift guard was tested by temporarily renaming `/support/` → `/support-renamed/`: `Error: llms.txt references 1 path(s) with no matching page in src/pages` — the build refused |
| 36 | On mobile (390 px), both hero CTAs extended into the WhatsApp/Call FAB column by 2–6 px, partially obscuring the play icon and right padding; the proof row (✓ بدون پیش پرداخت / ✓ مشاوره رایگان / ✓ ضمانت نتیجه) sat at the bottom of the viewport under both FABs | Added physical `mr-16 sm:mr-0` to the CTA flex container **and** the proof-row container. Logical `me-16` was tried first but in RTL it maps to `margin-left` and pushed content *toward* the FAB — confirmed by CDP measurement (right edge moved from 320 → 352). Switched to physical `mr-16` so the safe-area follows the FAB regardless of direction | CDP mobile re-measure: primary CTA right edge 320 → **288** (FAB at 314 → 26 px clearance), secondary 316 → **284** (30 px clearance); proof row right edge 366 → **298** (16 px clearance). Fresh screenshot: `audit-2026-09-12/shots-after/home-mobile-fixed.png` |
| 37 | Content typo «ابعادگیری تقویم» (not standard Persian) in the content-calendar workflow — step 3's own description says «پردازش ماهانه، هفتگی و روزانه», i.e. time breakdown | Replaced with «زمان‌بندی تقویم», which matches the step's description. Fixing the source fixes the visible step **and** the `HowTo` schema together, since both are generated from the same `workflowSteps` array | `dist/…/content-calendar/index.html`: 0 × «ابعادگیری», 2 × «زمان‌بندی تقویم» (markup + `HowToStep`) |
| 38 | **Foreign-script contamination in Persian copy** — found by scanning `src/` for CJK/Cyrillic/accented-Latin characters inside Persian strings: Russian «проблемы», «оптимال», «метод‌هایی»; Japanese «スパム»; Chinese «客户»; Spanish «estratégico». Plus «هزینه پز کسب مشتری» (garbled) and a missing space in «پکیج حرفه‌ای۱۲.۵ میلیون» | All 8 replaced with correct Persian: «شناسایی سریع مشکلات», «استفاده بهینه از کشینگ», «متدهایی», «ضد هرزنامه», «ساخت جامعه‌ای از مشتریان وفادار و فعال», «طراحی UX استراتژیک محور», «هزینه به‌ازای کسب مشتری (CPA)», «پکیج حرفه‌ای ۱۲.۵ میلیون» | Re-scan of all 45 built pages: **0** CJK / Cyrillic / accented-Latin characters remain |
| 39 | Testimonials and headline stats were hard-coded and scattered, so the authenticity problem could not be audited or corrected in one place | New `src/lib/site-claims.ts` holds all 6 testimonials and the 4 headline stats with an explicit `verified: false` flag and a documented path to compliance. `TestimonialsSection.astro` now renders from it with proper semantics: `<blockquote>` for the quote, `<cite>` for the author, `role="img"` + Persian-digit `aria-label` («امتیاز ۵ از ۵») for the stars, and a `<dl>` (dt/dd, reversed visually) for the stats. `WhyChooseUsSection.astro` reads the same numbers. **No `Review` schema was added** — emitting rating markup for unverifiable reviews is the violation, not the fix | Build 45/45, `astro check` 0 errors; `dist/index.html`: 6 `<blockquote>`, 6 `<cite>`, 6 correct aria-labels, 1 `<dl>` |
| 40 | The ~20 unverifiable stats turned out to be **297** numeric claims across the `.astro` marketing pages, 12 of them wrapped in guarantee language («ضمانت» / «تضمین» / «۱۰۰٪ اطمینان») | Generated `audit-2026-09-12/claims-to-verify.md` — a prioritised inventory with file:line for every claim, plus a "start here" table of the 3 cheapest high-impact fixes (notably the **default** meta description «رشد ۳۰۰٪ … را تضمین کنید», which only the homepage currently inherits). **Deliberately not rewritten**: deleting or substantiating these is a business/legal decision, and inventing replacement figures would just create new unverifiable claims | Report generated from a scripted scan, so it can be regenerated after edits |
| 41 | The default `og:image` was a **512×512** logo. Two pages (`/faq/`, `/services/`) declared it as `1200×630` — factually wrong metadata. A square logo is also a poor feed card for the `summary_large_image` Twitter card / LinkedIn / Telegram — the spec wants 1200×630 | Generated a real 1200×630 branded OG card by rendering an HTML template through headless Chrome using the site's actual Vazirmatn fonts (`regular.woff2` + `bold.woff2`) so Persian shaping is correct, captured at DSF 2 for crispness then downsampled to the spec size. Saved to `public/og-default.png` (506 KB). `BaseLayout.DEFAULT_OG_IMAGE` and the three explicit callers (`index`, `faq`, `services`) now point at the card | **0** pages still use `Logo-spaced.png` as OG image; **all 45** pages declare `og:image:width=1200, height=630` matching real image dimensions. The card source lives at `audit-2026-09-12/og-card-source/index.html` so it can be regenerated when branding changes |
| 42 | **Regression I introduced in fix #31:** when `blog/index.astro` moved off the deleted `blog-data.ts` override table onto `getCollection('blog')`, the `Blog` → `blogPost` nodes lost `datePublished`, `dateModified`, `author` and `image`. All 7 `BlogPosting` nodes on `/blog/` were emitting bare `headline` + `url`, which fails Google's BlogPosting guidelines. Static HTML checks never caught it — only a JSON-LD field sweep did | Each `blogPost` node now carries `headline`, `description`, `url`, `inLanguage`, `datePublished`, `dateModified`, `author` (`Person`, from frontmatter) and an absolute `image` URL. Optional fields are added conditionally so a post missing one still emits valid JSON | `dist/blog/index.html`: 7/7 `BlogPosting` nodes have `datePublished` + `author`; site-wide schema sweep reports **0** remaining field issues |
| 43 | **Dangling schema reference (also mine, from fix #14):** `/portfolio/` emitted `isPartOf: { '@type': 'WebSite', '@id': '…/#website' }` as an `@id`-only stub — but `/portfolio/` was the *only* page on the site mentioning `#website`, so the reference pointed at an entity defined nowhere | The `WebSite` node is now self-contained (`@id` + `name` + `url` + `inLanguage`), which both fixes the dangling reference and adds a proper `WebSite` entity | `dist/portfolio/index.html`: `WebSite` has `name` + `url`; sweep reports 0 dangling/incomplete entities |
| 44 | **Invalid HTML: 126 nested anchor → button pairs across 38 of 45 pages.** The source pattern `<a href class><Button>…</Button></a>` renders a `<button type="button">` *inside* an anchor — interactive content nested in interactive content, which is invalid (WCAG 4.1.2) and double-reports to screen readers. Pre-existing, not introduced by this session; missed by the build, by `astro check`, and by all 111 CDP tests. It surfaced only through a WCAG 2.5.8 tap-target probe reporting *nearest target 0 px* — two controls sharing one centre | Collapsed all 35 source wrappers across 14 files: the wrapper's `href` and `class` are merged into `Button` (which already renders an anchor when given an `href`) and the redundant wrapper is removed. Applied with a quote- and brace-aware tag scanner rather than a naive regex, so `>` inside attribute values and `{…}` expressions is handled correctly | `dist/`: **0** nested anchor → button (was 126). 382 key CTA hrefs (`/consultation/`, `/portfolio/`, `/services/seo/`) intact. Build 45/45, `astro check` 0 errors / 0 warnings, CDP **111/111**, links **0** broken, anchors **0**/180 broken |
| 45 | **One real WCAG 2.5.8 target-size failure** on `/blog/`: the «ادامه مطلب» ghost button was 90×20 px (`p-0 h-auto`), and because it sat inside a wrapper anchor its nearest neighbour was **0 px** away — two interactive targets sharing a centre. The other 7–9 undersized links per page **pass** the spacing exception (24 px circles centred on them do not intersect), so they were deliberately left alone rather than churned | The `Button` now carries the href directly (no wrapper) plus `min-h-[24px]` | Re-probe across `/`, `/blog/`, `/faq/`, `/portfolio/` at 390 px and 1440 px: **0 real violations**, with undersized-but-adequately-spaced targets correctly excluded |
| 46 | **Intent blur between `/services/ai/` and `/services/seo/`** — GSC query «خدمات سئو هوش مصنوعی» (pos 10, 65.2 impr) had both pages competing for it, with neither stating how the two actually differ. The page already had a "traditional vs AI marketing" table, but not the AI-SEO vs classic-SEO distinction | Added a comparison table on the AI page covering goal, keyword research, content production, update cadence and best-fit business, plus a closing note that cross-links `/services/seo/` (fix the foundation first) and `/services/content/` (for publishing cadence). **No statistics invented** — every row is a qualitative distinction | `dist/services/ai/index.html`: table present, both cross-links resolve. Build 45/45, `astro check` 0/0, CDP 111/111, links 0 broken |
| 47 | **«اتوماسیون انتشار محتوا» (16 impr) and «اتوماسیون چرخه محتوا» (9 impr) had no owning page** — the audit explicitly flagged this as a content gap, so the queries had no landing target | Added a 4-step «اتوماسیون چرخه تولید و انتشار محتوا» section to `/services/ai/content-creation/` (تولید → بازبینی → انتشار → توزیع), with step 3 linking to the content-calendar page. Deliberately no invented numbers | `dist/services/ai/content-creation/index.html`: section present, `/services/content/content-calendar/` link resolves. Page still has exactly 1 `<h1>`; all 45 pages re-verified at 1 `<h1>` each |
| 48 | **«استراتژی محتوا» (41 impr / pos 40.5) — the owner page answered the wrong question.** `/services/content/` had an AEO block answering «استراتژی محتوا **چیست**», but the query is «استراتژی محتوا شامل **چه مواردی است**» — what it *includes* | Added a 7-component section (پرسونا، خوشه‌بندی، ممیزی، قالب و کانال، تقویم، لحن برند، شاخص‌های اندازه‌گیری) with each component linking to the sub-service that owns it, making this page the hub. No invented statistics | `dist/services/content/index.html`: section present, both cross-links resolve |
| 49 | **«تقویم محتوایی سایت» and «اعتبار صفحه» had no image on their owning service pages**, though the audit required one. The assets already existed (`content-calendar-post-template.webp`, `content-authority-post-checklist.webp`, both 1672×941) but were referenced **only** by blog posts — so no new art was needed | Reused both assets on the owning service pages with descriptive Persian `alt`, native `width`/`height` (avoids CLS) and `loading="lazy"` | Both images render; **0** `<img>` site-wide missing `alt`/`width`/`height` (re-checked all 45 pages) |
| 50 | **«خدمات سوشال مدیا» (pos 11 — the audit's "nearest win") was thin on internal links.** Title work was already done; the remaining item was 2 outbound internal links from the owner page | Added a closing paragraph on `/services/marketing/social-media/` linking to social-media content production and the content calendar — genuine complements, not filler | Both links present and resolve; links 2588 → **0** broken, anchors 180 → **0** broken |
| 51 | **7 primary CTAs rendered as invisible white-on-white.** The `outline` button variant ships `bg-background` (white), and every dark CTA section passes `text-white`, so the label only appeared on hover. Pre-existing; invisible to the build, to `astro check` and to all 111 CDP tests. Note the contrast checker only caught **1** of the 7 — the other 6 sit on *gradient* sections, which the checker classifies as "uncertain" and skips | Added `bg-transparent` to all 7 (`ContactCTA`, `faq`, `services/web`, `services/content`, `services/ai`, `services/marketing`, `services/seo`) | Measured computed style on 8 pages: every one now `backgroundColor: rgba(0,0,0,0)` with `color: white`. The one solid-background case went from 1:1 to passing |
| 52 | **WCAG 1.4.3 colour-contrast failures found by a check that had never been run.** 112 failing instances across 11 distinct colour pairs: `text-green-600` on white (**3.22:1**, 34 instances), `text-primary` (blue-600) category badges on `bg-primary/10` (**4.487:1**, 24), `text-yellow-400` stars (1.57:1, 15), `text-purple-200` step numbers (1.31:1, 4) | `text-green-600` → `text-green-700` (4.95:1 on white, 4.50:1 on green-100 — passes on both); badge `text-primary` → `text-blue-700` (5.93:1); stars → `text-amber-600` (3.20:1, meets the 3:1 threshold for graphics); step numbers → `text-purple-500` (3.94:1) | Contrast sweep across all 44 pages: **112 → 48** instances. All remaining are documented below rather than churned |

**Contrast — remaining, with reasoning:**

| What | Ratio | Decision |
|---|---|---|
| `bg-green-100 text-green-700` badges (30) | **4.503:1** | **Passes** AA, but by 0.003. Left as-is; `green-800` would give 6.49 if you want headroom. |
| Rating stars `text-amber-600` on white (15) | 3.20:1 | Meets the **3:1** threshold for graphical objects (WCAG 1.4.11). They carry `role="img"` + Persian `aria-label`, so the rating is also exposed non-visually. Left at amber rather than brown `amber-700`. |
| Code-block comments (3) | 3.05:1 | **Accepted.** Every mainstream dark syntax theme fails here — I measured 8 (best: night-owl 3.87). Comments are deliberately dimmed; switching themes would restyle every code block with no real gain. |

**Build result:** 45 pages built (+ `llms.txt` / `llms-full.txt` endpoints), sitemap-index generated, ~0 KB client JS retained (~2.4 KB inline for header/anchors only).

### Structured-data sweep

Parsed all **119** `ld+json` blocks across the 45 pages: **0** JSON parse errors and **0** incomplete entities. Coverage includes 196 `Question`/`Answer` pairs across 34 `FAQPage` blocks, 92 `Service`, 66 `Offer`, 32 `BreadcrumbList`, 21 `Person`, 14 `BlogPosting`, 8 `SpeakableSpecification`, 6 `HowToStep`.

One flagged item was judged **not** a defect and left alone: `Offer` nodes carry no `price`. Price is optional for `Service` offers, and inventing prices for 66 quote-based services would publish wrong numbers — the same reasoning as the unverifiable-stats decision (fix #40).

### Runtime verification (headless Chrome over CDP)

Static HTML checks can't prove interactive behaviour, so every JS change was executed in a real browser against the built `dist/` — **111/111 assertions pass** across 12 pages:

- Accordion (all 7 blog posts + `/faq/`): `aria-expanded` matches panel visibility on load, every `aria-controls` resolves to a uniquely-id'd `role="region"` panel labelled by its trigger, opening one item closes the others, and clicking an open item collapses it.
- Mobile menu (`/`): opens with `aria-expanded="true"`, focus moves into the panel, Tab wraps at both ends, Escape closes **and** returns focus to the trigger, clicking outside closes.
- Every page: no JS exceptions or console errors, no duplicate element ids, no dangling `aria-controls`/`aria-labelledby`/`aria-describedby`, exactly one `<h1>`, every `<img>` has `width`+`height`.

Two initial failures were **test artifacts**, not product bugs: a blog post hosts two independent accordions (TOC + FAQ), so the old assertion was comparing across widgets; and `/404/` logs its own 404 status, which the harness counted as an error. The accordion "2nd item resets 1st" failure on `/blog/ai-seo-guide/` was also caused by `faq-0` starting open — clicking it *closed* it.

### UI/UX re-audit at 1440×900 + 390×844

Also re-measured the audit's UI items against the live render:

| Audit finding | Status |
|---|---|
| §6.4 ~40 % hero dead space | **Resolved.** Hero now fills the viewport — desktop 918/900, mobile 1060/844, only 64 px of padding at the bottom |
| §6.5 No homepage imagery | **Resolved** by `PortfolioPreview` (5 real images, fix #25) |
| §6.6 Stray particle artifacts | **Resolved.** Remaining `animate-float` elements are the intentional blurred gradient mesh (3 large blobs) and the scroll-down indicator — they read as design, not rendering bugs. Fresh screenshot: `audit-2026-09-12/shots-after/home-desktop-final.png` |
| §6.7 RTL CTA hierarchy inverted | **Already correct** — verified at runtime: in the flex row, primary «مشاوره رایگان دریافت کنید» sits at x ≈ 728 (visual right), outline «مشاهده نمونه کارها» at x ≈ 462 (visual left), with `direction: rtl` + `flex-direction: row` putting the first DOM child at the visual start. Audit item may predate this state |
| §6.8 Illegible logo mark | Subjective; no measurement. Left untouched |
| §6.9 FABs collide with content on mobile | **Resolved** by fix #36 |
| §6.11 Header CTA label/route mismatch | **Resolved** by fix #4 |

## Out-of-repo blockers (need you to act)

These cannot be fixed by code in this repo:

| Finding | What to do |
|---|---|
| **Cloudflare "Content Signals Policy" is replacing `robots.txt`** | In the Cloudflare dashboard for this Pages project, disable the managed Content Signals feature (or switch to "auto" so the file in `public/robots.txt` is served as-is). Otherwise your sitemap directive, GPTBot/PerplexityBot/ClaudeBot/Google-Extended allowances, and `User-agent: *` rules stay invisible. |
| **The live contact form returns 500** (`{"error":"Missing API key … Resend"}`) | The Next.js production deployment is missing its `RESEND_API_KEY` env var. Set it, then **before deploying Astro** also attach Cloudflare's `EMAIL` binding in the dashboard — the `functions/api/send-email.ts` handler returns 500 when `env.EMAIL` is missing. The `wrangler.toml` comment already warns this isn't auto-attached. |
| **The Astro build is not deployed** | Production still serves the legacy Next.js app from `main` (verified: `/_next/static/*`, 482 KB HTML, `/_astro/*.css` → 404, `/llms.txt` → 404). Wire the Cloudflare Pages production deployment to `astro-migration` (or merge to `main`). |
| **Live has duplicate H1s and almost no schema** | Resolved by deploying Astro — once live, every page will have 1 H1, canonical, and the full schema coverage from `dist/`. |

## Carried over (not safe to "fix" without your decision)

### Resolved in this pass

- ~~Unsourced `AggregateRating` + `۴.۹ از ۵ امتیاز`~~ → **done** (fix #18). Still worth checking **Search Console → Manual Actions** once, because the rich result may already have been flagged before this change.
- ~~Possible content typo «ابعادگیری تقویم»~~ → **done** (fix #37): now «زمان‌بندی تقویم», matching the step's own description; the `HowTo` schema picked it up automatically because both come from the same array.
- ~~Stale `dateModified` on 5 of 7 blog posts~~ → **done honestly** (fix #19). Real ۱۴۰۵ update sections were written first, *then* `updated` was set — no date spoofing.

### Still open

| Finding | Why deferred |
|---|---|
| Unverifiable stats across service pages — **now inventoried: see `claims-to-verify.md` (fix #40)** (`۵۰۰+` projects, `۹۸%`/`۹۴%`/`۹۲%`/`۱۰۰%` satisfaction, `۱۸۰%` increases, `۵+` years, "ضمانت نتیجه") | Same class of risk as the rating that was just removed, but far more widespread than assumed: **297** claims across `src/**/*.astro`, not ~20 — all inventoried with file:line in `claims-to-verify.md`. Need real numbers + legal sign-off before touching them. Give me the true figures and I'll swap them in, or say "delete" and I'll remove them. |
| **⚠️ Homepage testimonials — now centralised in `src/lib/site-claims.ts` (fix #39), authenticity still unconfirmed** | `TestimonialsSection.astro` renders **6 named people at named companies, each with a 5-star rating** and specific outcome claims («فروش آنلاین ما ۳۰۰٪ افزایش پیدا کرد»). This is the same sourcing problem as the `AggregateRating`, but with real individuals and businesses attached. Good news: it emits **no** `Review`/`AggregateRating` schema, so there is no structured-data violation right now. I did **not** touch it — these may be genuine clients. **Please confirm they're real and authorised.** If they are, add `Review` schema so the markup matches; if they're placeholders, they should go for the same reason the ۴.۹ did. |
| Duplicate/oversized meta descriptions | `/404.html` inherited BaseLayout's hardcoded default description — identical to the homepage's — and `/blog/seo-guide/` had only an ۸۶-char `excerpt` with no `description` | Gave `404.astro` its own description; added a ۱۵۱-char `description` to `seo-guide.mdx` | **0 duplicate titles, 0 duplicate descriptions**, all titles ≤۶۰, all descriptions ۱۰۰–۱۶۵ |
| HTML caching rule in `public/_headers` doesn't match extensionless Pages URLs | Needs Cloudflare rule tweak outside this repo (or matching on `/`). |
| `HowTo` rich results | Note: Google **retired HowTo rich results** (2023), so don't expect a visual SERP change. It's still worth having for Bing, and more importantly for AEO/GEO — it's the clearest machine-readable statement of your process. |

## Files changed

- `src/components/ui/button/Button.astro`
- `src/components/ui/Icon.astro`
- `src/components/Home/HeroSection.astro`
- `src/components/Home/AllServicesSection.astro`
- `src/components/Home/ServicesSection.astro`
- `src/components/Home/CTASection.astro`
- `src/components/Layout/Header.astro`
- `src/components/Services/ServicesHero.astro`
- `src/components/Services/ContactCTA.astro`
- `src/components/Services/ServicesList.astro`
- `src/components/Services/PricingSection.astro`
- `src/layouts/BaseLayout.astro`
- `src/styles/globals.css`
- `src/pages/consultation/index.astro`
- `src/pages/contact/index.astro`
- `src/pages/portfolio/index.astro`
- `src/pages/services/seo/content-authority/index.astro`
- `src/pages/services/ai/marketing-engagement/index.astro`
- `src/pages/services/ai/analysis-strategy/index.astro`
- `src/pages/services/ai/content-creation/index.astro`
- `src/pages/services/content/content-calendar/index.astro`
- `src/components/HomepageStructuredData.astro`
- `src/pages/privacy/index.astro`
- `src/pages/terms/index.astro`
- `functions/api/send-email.ts`
- `astro.config.mjs` (sitemap `lastmod`)
- `src/pages/blog/index.astro` (collection-driven listing)
- `src/pages/blog/[slug].astro` (frontmatter is now the source of truth)
- `src/content/blog/ai-seo-guide.mdx`
- `src/content/blog/digital-marketing-trends.mdx` (renamed from `-2024`)
- `src/content/blog/google-ads-guide.mdx` (renamed from `-2024`)
- `src/content/blog/seo-faq-guide.mdx`
- `src/content/blog/seo-guide.mdx` (renamed from `-2024`)
- `src/content/blog/content-calendar-guide.mdx`
- `src/content/blog/page-authority-guide.mdx` (TOC anchors only)
- `src/content.config.ts` (`keyTakeaways` / `faqs` in schema)
- `public/js/ajax-form.js` (surfaces server validation messages)
- `public/robots.txt`, `public/_headers`, `public/_redirects`
- `src/components/Home/TestimonialsSection.astro` (rewritten: `<blockquote>`/`<cite>`/`<dl>`, accessible ratings)
- `src/components/Home/WhyChooseUsSection.astro` (reads shared stats)
- `src/components/Home/HeroSection.astro` (mobile FAB clearance, fix #36)
- `src/components/Layout/Header.astro` (focus restore + outside-click close, fix #32)
- `src/pages/services/content/content-calendar/index.astro` (typo, fix #37)
- `src/pages/services/web/website-speed/index.astro`, `src/pages/services/web/ux-architecture/index.astro`, `src/pages/services/marketing/email-marketing/index.astro`, `src/pages/services/marketing/integrated-campaigns/index.astro`, `src/pages/services/web/seo-web-design/index.astro`, `src/pages/services/web/landing-pages/index.astro` (foreign-script contamination, fix #38)
- `src/layouts/BaseLayout.astro` (1200×630 default OG image, fix #41)
- `src/pages/index.astro`, `src/pages/faq/index.astro`, `src/pages/services/index.astro` (corrected `og:image` dimensions, fix #41)
- `public/og-default.png` (new — generated 1200×630 social card)
- `src/lib/site-claims.ts` (new — testimonials + headline stats with `verified` flags)
- `src/lib/llms.ts`, `src/pages/llms.txt.ts`, `src/pages/llms-full.txt.ts` (new — generated llms files, fix #35)
- `src/components/Home/PortfolioPreview.astro` (new)
- `audit-2026-09-12/claims-to-verify.md` (new — 297-claim inventory, fix #40)
- `audit-2026-09-12/og-card-source/index.html` (new — regeneratable OG card source)
- **deleted:** `src/lib/blog-data.ts` (contradicted MDX frontmatter — see fix #22), `src/lib/blog.ts` (unused after the frontmatter migration), `public/sw.js` (orphaned), `public/llms.txt` + `public/llms-full.txt` (replaced by generated endpoints)