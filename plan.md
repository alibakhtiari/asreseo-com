# برنامه جامع بهینه‌سازی سئو، AEO و GEO وب‌سایت عصر سئو (asreseo.com)
## بر اساس تحلیل داده‌های عملکردی سرچ کنسول گوگل (GSC) و ممیزی کامل تمامی صفحات

این برنامه جامع بر اساس ممیزی دقیق تمامی صفحات، سرویس‌ها، مقالات وبلاگ، اسکیماها، سوالات متداول (FAQ)، ساختار تصاویر و آلت‌تگ‌ها در کنار واکاوی داده‌های فایل اکسل سرچ کنسول گوگل (`asreseo.com-Performance-on-Search-2026-09-08.xlsx`) تدوین شده است تا وب‌سایت [عصر سئو](https://asreseo.com) را در سه حوزه **SEO** (موتورهای جستجوی سنتی گوگل/بینگ)، **AEO** (پاسخ‌دهی مستقیم و Featured Snippets) و **GEO** (موتورهای جستجوی مولد مانند Perplexity، ChatGPT Search، Gemini و Claude) به حداکثر رشد ترافیک و رتبه برساند.

---

## ۱. تحلیل جامع داده‌های سرچ کنسول (GSC) و وضعیت فعلی

### ۱.۱. شاخص‌های کلیدی عملکرد (۳ ماه گذشته)
* **کل کلیک‌ها (Total Clicks):** ۱,۰۹۲ کلیک
* **کل نمایش‌ها (Total Impressions):** ۱,۸۴۸ نمایش
* **دستگاه‌ها:** ۹۴۵ کلیک روی موبایل (۸۶.۵٪) | ۱۴۷ کلیک روی دسکتاپ (۱۳.۵٪) — *ضرورت تمرکز ۱۰۰٪ روی Mobile-First UX و خوانایی سریع*.
* **کشورها:** ایران (۱,۰۹۲ کلیک)، آمریکا (۱۱ نمایش)، افغانستان (۳ نمایش)، انگلستان (۲ نمایش).
* **وضعیت نتایج غنی (Search Appearance):** **کاملاً خالی (۰ نمایش Rich Results)!** هیچ‌کدام از صفحات سایت به دلیل نقایص ساختاری اسکیما نتوانسته‌اند ریچ اسنیپت (FAQ، ستاره، بردکرامب) دریافت کنند.

### ۱.۲. تحلیل صفحات پربازدید و فرصت‌های طلایی
| صفحه | کلیک | ایمپرشن | جایگاه میانگین | وضعیت فعلی و تحلیل فرصت |
| :--- | :---: | :---: | :---: | :--- |
| **`/services/ai/`** | **۱,۰۸۷** | **۱,۲۷۰** | **۵۰.۷۵** | **موتور اصلی ترافیک (۹۹.۵٪ کلیک‌ها)**. کلمات اصلی `خدمات هوش مصنوعی` و `خدمات هوش مصنوعی و سئو`. صفحه در حال حاضر **فاقد هرگونه اسکیما، فاقد بخش FAQ و فقط با ۱ تصویر** است. با ارتقا به پوزیشن زیر ۱۰ ترافیک می‌تواند ۵ الی ۱۰ برابر شود. |
| **`/` (صفحه اصلی)** | **۴** | **۱۹** | **۴.۳۷** | تایتل ناقص است (در کد صراحتاً پاس داده نشده و روی پیش‌فرض می‌افتد)، فاقد WebSite SearchAction schema و نیازمند تقویت لینک‌دهی به خوشه‌های موضوعی است. |
| **`/services/ai/content-creation/`** | **۱** | **۳** | **۵.۶۷** | پتانسیل بالا در کوئری‌های تولید محتوای هوشمند؛ فاقد اسکیما FAQ و فاقد تصاویر اینفوگرافیک فرآیند. |
| **`/services/content/content-calendar/`** | **۰** | **۱۱۰** | **۶۳.۶۸** | **فرصت طلایی فوری:** کوئری‌های `تقویم محتوا` (۳۵ امپ)، `تقویم محتوایی سایت` (۲۰ امپ)، `اتوماسیون انتشار محتوا` (۱۶ امپ). اسکیما FAQ در این صفحه به شکل غیراستاندارد نوشته شده و خوانده نمی‌شود. |
| **`/services/seo/content-authority/`** | **۰** | **۸۰** | **۵۱.۲۴** | کوئری مهم `اعتبار صفحه` (۱۸ امپ). تایتل و محتوا روی این کلمه کلیدی بهینه‌سازی نشده است. |
| **`/faq/`** | **۰** | **۲۵** | **۴۰.۱۶** | کوئری `سوالات متداول سئو` (۱۷ امپ). صفحه فقط ۴ سوال دارد! بسیار لاغر و بدون دسته‌بندی موضوعی. |
| **`/about/`** | **۰** | **۱۷** | **۳.۹۴** | برای کوئری‌های برندی؛ فاقد اسکیمای AboutPage و فاقد اطلاعات اعضای تیم (E-E-A-T). |
| **`/blog/`** | **۰** | **۱۰** | **۱۳.۳** | تنها ۳ مقاله دارد که همگی تاریخ ۲۰۲۴ دارند و تصاویر آن‌ها `placeholder.svg` است! هیچ مقاله‌ای درباره هوش مصنوعی، تقویم محتوا یا اعتبار صفحه وجود ندارد. |
| **`/services/content/`** | **-** | **-** | **-** | **خطای بحرانی ۴۰۴!** پوشه `src/pages/services/content/index.astro` وجود ندارد در حالی که از نقشه سایت و هدر به آن لینک داده شده است. |

---

## ۲. ممیزی جامع ساختاری (Technical, On-Page, Schema, Images)

### ۲.۱. نقص بحرانی در اسکیماهای FAQPage (چرا ریچ اسنیپت فعال نشده است؟)
در ۱۴ صفحه از خدمات سایت (مانند `content-calendar`، `technical-onpage`، `social-media-content` و...) اسکیما به این شکل نادرست نوشته شده است:
```javascript
// ❌ ساختار نامعتبر در گوگل:
const structuredData = {
  '@context': 'https://schema.org',
  '@type': 'Service',
  // ...
  'faqSection': { // ویژگی غیرمجاز در نوع Service!
    '@type': 'FAQPage',
    'mainEntity': [...]
  }
}
```
**نتیجه:** ابزار اعتبارسنجی ریچ ریزالت گوگل، ویژگی نامعتبر `faqSection` در نوع `Service` را به عنوان `FAQPage` مستقل تشخیص نمی‌دهد و رد می‌کند.
**راهکار AEO:** اسکیما باید به صورت دو تگ JSON-LD جداگانه یا یک آرایه `@graph` استاندارد درج شود:
```javascript
// ✅ ساختار استاندارد مورد تایید گوگل:
const schemas = [
  { '@context': 'https://schema.org', '@type': 'Service', ... },
  { '@context': 'https://schema.org', '@type': 'FAQPage', 'mainEntity': [...] },
  { '@context': 'https://schema.org', '@type': 'BreadcrumbList', 'itemListElement': [...] }
];
```

### ۲.۲. وضعیت اسکیما در سایر بخش‌ها
1. **۱۵ صفحه بدون اسکیما:** از جمله مهم‌ترین صفحه سایت `/services/ai/`، هاب‌های اصلی `/services/seo/`، `/services/marketing/`، `/services/web/`، صفحات `/about/`، `/consultation/`، `/portfolio/` و... هیچ اسکیمایی ندارند.
2. **بردکرامب ناقص:** کامپوننت `BreadcrumbStructuredData.astro` فقط یک آیتم (خانه) دارد که طبق مستندات گوگل فاقد ارزش سلسله‌مراتبی است.
3. **صفحه اصلی:** نیاز به تزریق اسکیمای `WebSite` همراه با `SearchAction` و به‌روزرسانی اطلاعات سازمان دارد.

### ۲.۳. ممیزی تصاویر و آلت‌تگ‌ها (Image SEO)
1. **تگ آلت یکسان و تکراری:** تگ `alt` تصاویر شبکه‌های اجتماعی و OpenGraph در ۲۵ صفحه دقیقاً عبارت تکراری `alt: "خدمات عصر سئو"` است!
2. **فقدان تصاویر داخلی:** صفحات لندینگ فقط یک تصویر هدر (Hero) دارند و در بدنه متن هیچ دیاگرام، اینفوگرافیک یا تصویر توضیحی برای نگه‌داشت کاربر و ایندکس Google Images وجود ندارد.
3. **تصاویر وبلاگ:** تمام مقالات وبلاگ در فایل‌های MDX از `image: "/placeholder.svg"` استفاده می‌کنند.

### ۲.۴. ممیزی محتوایی و فرصت‌های AEO / GEO
* **AEO (پاسخ به موتورهای پرسش):** فقدان پاراگراف‌های تعریفی کوتاه (Direct Answer Boxes با طول ۴۰ تا ۶۰ کلمه) در ابتدای بخش‌های هدر ۲.
* **GEO (موتورهای زاینده هوش مصنوعی):** فقدان جداول مقایسه‌ای شفاف، مراحل گام‌به‌گام شماره‌دار (`<ol>`)، نقل‌قول‌های آماری دقیق با بنچمارک‌های عددی، و نقایص فایل `llms.txt`.

---

## ۳. برنامه عملیاتی تفصیلی برای هر صفحه / سرویس / وبلاگ

### فاز ۱: برطرف‌سازی خطاهای بحرانی معماری و اسکیما
1. **[NEW] ایجاد صفحه هاب محتوا `src/pages/services/content/index.astro`**:
   - رفع خطای ۴۰۴ برای کاربر و ربات‌های گوگل.
   - معرفی تمام زیرشاخه‌های محتوایی (متنی، تقویم محتوا، شبکه‌های اجتماعی، تصویری، ترجمه).
   - افزودن اسکیماهای کامل `Service` و `BreadcrumbList`.
2. **اصلاح و تفکیک اسکیماها در تمام ۲۵ صفحه خدمات**:
   - اصلاح ساختار `FAQPage` از حالت تودرتوی غیرمعتبر به اسکیماهای سطح بالا.
   - پوشش ۱۰۰٪ سوالات درج شده در ظاهر صفحه در داخل اسکیما (در حال حاضر صفحات ۶ سوال دارند اما در اسکیما فقط ۱ سوال درج شده است!).
3. **به‌روزرسانی و تکمیل `public/llms.txt` و ایجاد `public/llms-full.txt`**:
   - افزودن معرفی دقیق ساختار عصر سئو به عنوان مرجع سئو و هوش مصنوعی در ایران جهت استناد چت‌بات‌ها (ChatGPT, Perplexity, Gemini).

---

### فاز ۲: بهینه‌سازی خوشه طلایی هوش مصنوعی (AI Services) — اولویت اول درآمد و ترافیک
با توجه به اینکه کوئری‌های `خدمات هوش مصنوعی` (۹۹۵ کلیک، پوزیشن ۴۷.۵) و `خدمات هوش مصنوعی و سئو` (۸۸ کلیک، پوزیشن ۹۱.۹) در این خوشه هستند، این صفحات نیاز به جراحی اساسی دارند:

#### ۱. صفحه اصلی هوش مصنوعی (`src/pages/services/ai/index.astro`):
* **تایتل جدید سئومحور:**
  `خدمات هوش مصنوعی و سئو سازمانی | شرکت خدمات هوش مصنوعی | عصر سئو` (۶۴ کاراکتر)
* **دیسکریپشن جدید:**
  `ارائه تخصصی‌ترین خدمات هوش مصنوعی و سئو در ایران. پیاده‌سازی اتوماسیون سازمانی، چت‌بات هوشمند و تولید محتوای AI با مشاوره رایگان عصر سئو.` (۱۴۳ کاراکتر)
* **تغییرات محتوایی و تیترها:**
  - H1: `خدمات هوش مصنوعی؛ تحول دیجیتال و رشد سئو با AI`
  - افزودن بخش AEO: پاسخ مستقیم به «هوش مصنوعی چگونه رتبه سئو سایت را متحول می‌کند؟» در ۵۰ کلمه.
  - افزودن بخش اختصاصی برای کوئری‌های GSC: `خدمات هوش مصنوعی و سئو`، `شرکت خدمات هوش مصنوعی`.
  - افزودن بخش سوالات متداول (حداقل ۵ سوال کلیدی شامل هزینه، امنیت داده‌ها، ابزارهای مورد استفاده).
* **اسکیماها:**
  - تزریق اسکیمای `Service` کامل با ارائه دهنده عصر سئو، `OfferCatalog` و لیست خدمات.
  - تزریق اسکیمای `FAQPage` با ۵ پرسش و پاسخ.
  - تزریق اسکیمای `BreadcrumbList` (خانه > خدمات > خدمات هوش مصنوعی).
* **تصاویر و آلت‌تگ‌ها:**
  - بهینه‌سازی آلت هیرو: `alt="خدمات هوش مصنوعی و سئو سازمانی عصر سئو"`
  - افزودن حداقل ۲ تصویر اینفوگرافیک:
    1. دیاگرام چرخه ادغام AI در فرآیندهای سئو و مارکتینگ (`ai-seo-workflow.webp`).
    2. جدول مقایسه‌ای عملکرد کسب‌وکار قبل و بعد از پیاده‌سازی هوش مصنوعی (`ai-growth-comparison.webp`).

#### ۲. زیرصفحات هوش مصنوعی:
* **تولید محتوای هوشمند (`services/ai/content-creation/`):**
  - تارگت کوئری: `اتوماسیون چرخه محتوا` و `تولید محتوا با هوش مصنوعی`.
  - افزودن اسکیما `FAQPage` با ۵ پرسش درباره کیفیت مقالات هوش مصنوعی، جریمه نشدن توسط گوگل و بررسی انسانی.
  - آلت تصاویر اختصاصی: `تولید محتوای متنی و ویدیویی با هوش مصنوعی`.
* **تحلیل و استراتژی هوشمند (`services/ai/analysis-strategy/`):**
  - تارگت کوئری: `خدمات سئو هوش مصنوعی` و `سئو هوشمند`.
  - افزودن اسکیما `FAQPage`، جدول مقایسه ابزارهای تحلیلی سنتی در برابر AI.
* **بازاریابی و تعامل هوشمند (`services/ai/marketing-engagement/`):**
  - تارگت کوئری: `چت‌بات هوشمند فارسی` و `اتوماسیون بازاریابی AI`.
  - افزودن نمونه مکالمه چت‌بات و اسکیماهای لازم.

---

### فاز ۳: بهینه‌سازی صفحات با ایمپرشن بالا و کلیک صفر (Low-Hanging Fruits)

#### ۱. صفحه تقویم محتوایی (`src/pages/services/content/content-calendar/index.astro`):
* **وضعیت در GSC:** ۱۱۰ ایمپرشن، پوزیشن ۶۳. کوئری‌ها: `تقویم محتوا`، `تقویم محتوایی سایت`، `اتوماسیون انتشار محتوا`.
* **تایتل جدید:**
  `تقویم محتوایی سایت و شبکه‌های اجتماعی | برنامه‌ریزی هوشمند محتوا | عصر سئو` (۷۰ کاراکتر)
* **دیسکریپشن جدید:**
  `طراحی و تدوین تقویم محتوایی سایت و سوشال مدیا با اتوماسیون انتشار منظم. افزایش تعامل و سازماندهی تیم تولید محتوا با متدولوژی عصر سئو.` (۱۴۲ کاراکتر)
* **اقدامات محتوایی و AEO:**
  - بازنویسی تیترها برای تطابق دقیق با `تقویم محتوایی سایت` و `اتوماسیون انتشار محتوا`.
  - افزودن باکس دانلود یا پیش‌نمایش تمپلیت تقویم محتوا به عنوان آهنربای لید (Lead Magnet).
  - اصلاح اسکیما: انتقال ۶ سوال موجود در صفحه به یک تگ مستقل `FAQPage` JSON-LD.
  - افزودن تصویر اینفوگرافیک چرخه تقویم محتوا و اتوماسیون انتشار (`content-calendar-workflow.webp`).

#### ۲. صفحه سئو محتوا و اعتبار سایت (`src/pages/services/seo/content-authority/index.astro`):
* **وضعیت در GSC:** ۸۰ ایمپرشن، پوزیشن ۵۱. کوئری کلیدی: `اعتبار صفحه` (Page Authority).
* **تایتل جدید:**
  `افزایش اعتبار صفحه و دامنه | سئو محتوا و استراتژی تاپیکال آتوریتی | عصر سئو` (۷۰ کاراکتر)
* **دیسکریپشن جدید:**
  `خدمات افزایش اعتبار صفحه (Page Authority) و اعتبار دامنه با استراتژی سئو محتوا، تاپیکال آتوریتی و لینک‌سازی اصولی برای تسخیر رتبه‌های اول گوگل.` (۱۴۸ کاراکتر)
* **اقدامات محتوایی و AEO:**
  - افزودن بخش مجزا: «اعتبار صفحه (Page Authority) چیست و چه تفاوتی با اعتبار دامنه (DA) دارد؟» (پاسخ مستقیم برای AEO).
  - اصلاح اسکیما و ثبت سوالات متداول با محوریت افزایش پیج آتوریتی و لینک‌سازی.

#### ۳. صفحه سوالات متداول (`src/pages/faq/index.astro`):
* **وضعیت در GSC:** ۲۵ ایمپرشن، پوزیشن ۴۰. کوئری کلیدی: `سوالات متداول سئو`.
* **تایتل جدید:**
  `سوالات متداول سئو و دیجیتال مارکتینگ | پاسخ جامع به پرسش‌های کلیدی | عصر سئو`
* **دیسکریپشن جدید:**
  `پاسخ به مهم‌ترین سوالات متداول سئو، هزینه خدمات سئو، زمان‌بندی رتبه‌گیری، هوش مصنوعی در دیجیتال مارکتینگ و طراحی سایت توسط متخصصین عصر سئو.`
* **اقدامات محتوایی:**
  - افزایش سوالات از ۴ سوال به حداقل **۱۶ سوال جامع** در ۴ دسته‌بندی تب‌بندی شده:
    1. سوالات متداول سئو و رتبه‌بندی در گوگل
    2. سوالات خدمات هوش مصنوعی و اتوماسیون
    3. سوالات تبلیغات گوگل ادز و بازاریابی
    4. سوالات طراحی وب‌سایت و سرعت
  - به‌روزرسانی اسکیمای `FAQPage` با تمام ۱۶ سوال.

---

### فاز ۴: بهینه‌سازی صفحات پایه، سازمانی و خدمات دیگر

1. **صفحه اصلی (`src/pages/index.astro`):**
   - تعیین تایتل صریح: `آژانس سئو و هوش مصنوعی | خدمات دیجیتال مارکتینگ | عصر سئو`.
   - افزودن اسکیمای `WebSite` با قابلیت جستجو (`potentialAction: SearchAction`).
   - بهبود آلت تصاویر بخش‌های میانی صفحه.
2. **صفحه درباره ما (`src/pages/about/index.astro`):**
   - افزودن اسکیمای `AboutPage` متصل به `Organization`.
   - درج سوابق، تخصص‌ها و سیگنال‌های اعتماد E-E-A-T.
   - اصلاح آلت تصویر هیرو و افزودن تصاویر محیط کاری یا تیم متخصص.
3. **صفحه هاب خدمات سئو (`src/pages/services/seo/index.astro`):**
   - تزریق اسکیمای کامل `Service` و `BreadcrumbList`.
   - افزودن بخش FAQ و پوشش کوئری `خدمات سئو حرفه‌ای`.
4. **صفحه هاب خدمات مارکتینگ و وب (`services/marketing/` و `services/web/`):**
   - افزودن اسکیماهای missing به هاب‌ها.
   - اصلاح آلت‌های تکراری `alt: "خدمات عصر سئو"` در تمامی فایل‌ها به متون اختصاصی.

---

### فاز ۵: بازسازی وبلاگ و ایجاد مقالات هدفمند بر اساس کوئری‌های GSC

#### ۱. اصلاح ۳ مقاله موجود در وبلاگ:
* جایگزینی `image: "/placeholder.svg"` در تمام فایل‌های MDX با تصاویر اختصاصی WebP با کیفیت بالا و متناسب با موضوع.
* به‌روزرسانی سال و محتوا از حالت منقضی ۲۰۲۴ به محتوای همیشه سبز (Evergreen) و معتبر.
* بررسی و اطمینان از خروجی معتبر اسکیمای `Article` و `BreadcrumbList` در کامپوننت `BlogPost.astro`.

#### ۲. تدوین و تولید ۴ مقاله راهبردی جدید منطبق با کوئری‌های سرچ کنسول:
1. **مقاله ۱ (کوئری‌های `خدمات هوش مصنوعی و سئو` و `خدمات سئو هوش مصنوعی`):**
   - **اسلاگ:** `ai-seo-guide`
   - **عنوان:** `سئو و هوش مصنوعی: راهنمای عملی استفاده از AI برای کسب رتبه اول گوگل`
   - هدف: جذب ترافیک روی کوئری‌های پرجستجوی مرتبط با هوش مصنوعی و هدایت به لندینگ `/services/ai/`.
2. **مقاله ۲ (کوئری‌های `تقویم محتوا`، `تقویم محتوایی سایت`، `اتوماسیون انتشار محتوا`):**
   - **اسلاگ:** `content-calendar-guide`
   - **عنوان:** `راهنمای جامع تدوین تقویم محتوایی سایت و شبکه‌های اجتماعی + دانلود تمپلیت رایگان`
   - هدف: تصاحب رتبه ۱ تا ۳ برای کوئری‌های با ۱۱۰ ایمپرشن و هدایت به لندینگ `/services/content/content-calendar/`.
3. **مقاله ۳ (کوئری `اعتبار صفحه` / Page Authority):**
   - **اسلاگ:** `page-authority-guide`
   - **عنوان:** `اعتبار صفحه (Page Authority) چیست؟ تفاوت با Domain Authority و راه‌های افزایش آن`
   - هدف: جذب کوئری‌های تخصصی سئوکاران و مدیران سایت و لینک‌دهی به `/services/seo/content-authority/`.
4. **مقاله ۴ (کوئری `سوالات متداول سئو`):**
   - **اسلاگ:** `seo-faq-guide`
   - **عنوان:** `پاسخ به ۳۰ سوال متداول سئو که مدیران کسب‌وکار باید بدانند`
   - هدف: تسخیر Featured Snippets در گوگل و تقویت ساختار داخلی صفحه `/faq/`.

---

## ۴. استراتژی اختصاصی AEO و GEO

### ۴.۱. الزامات بهینه‌سازی موتورهای پاسخگو (AEO):
* **تگ‌گذاری مستقیم پرسش‌ها:** تمام پرسش‌های پرتکرار کاربران در فرمت `H2` یا `H3` با ساختار شفاف (مانند: «هزینه سئو سایت چقدر است؟») فرموله می‌شوند.
* **پاراگراف پاسخ صریح (Answer Capsule):** در نخستین پاراگراف بلافاصله پس از هدینگ، پاسخی مستقیم و قاطع در ۳ الی ۴ جمله ارائه خواهد شد.
* **لیست‌های گام‌به‌گام (`<ol>`):** برای فرآیندها و آموزش‌ها جهت شانس حداکثری در تصاحب Featured Snippets گوگل.

### ۴.۲. الزامات بهینه‌سازی موتورهای هوش مصنوعی مولد (GEO):
* **Entity Clarity (وضوح موجودیت):** اتصال صریح آژانس عصر سئو به موجودیت‌های شناخته‌شده در حوزه سئو، دیجیتال مارکتینگ و هوش مصنوعی در ایران در اسکیماها (`sameAs` معتبر به شبکه‌های اجتماعی، نشانی ثبت‌شده و شماره تماس رسمی).
* **فایل `llms.txt` و `llms-full.txt`:** به‌روزرسانی کامل فایل در ریشه سایت با خلاصه تفصیلی و دقیق تمامی ۲۵ خدمت و مقالات جدید وبلاگ، با نگارش ساختاریافته به گونه‌ای که هنگام استفاده پرامپت‌های وب توسط مدل‌های زبانی به راحتی استخراج و استناد (Citation) شوند.
* **داده‌های آماری و بنچمارک:** استفاده از آمار و ارقام مستند و جداول شفاف جهت تسهیل بازخوانی دیتای وب‌سایت در چت‌بات‌ها.

---

## ۵. ماتریس فایل‌های مورد ویرایش و ایجاد

### ۵.۱. فایل‌های جدید [NEW]
* `src/pages/services/content/index.astro` (صفحه هاب بازاریابی و تولید محتوا — رفع ارور ۴۰۴)
* `src/content/blog/ai-seo-guide.mdx` (مقاله جامع سئو و هوش مصنوعی)
* `src/content/blog/content-calendar-guide.mdx` (مقاله جامع تقویم محتوایی)
* `src/content/blog/page-authority-guide.mdx` (مقاله جامع اعتبار صفحه)
* `src/content/blog/seo-faq-guide.mdx` (مقاله ۳۰ سوال متداول سئو)
* `public/llms-full.txt` (نسخه مشروح داکیومنت هوش مصنوعی برای GEO)

### ۵.۲. فایل‌های مورد تغییر و بهینه‌سازی [MODIFY]
* **هسته و ساختار:**
  - `src/layouts/BaseLayout.astro` (پشتیبانی استاندارد از تزریق اسکیما و متاتگ‌های تکمیلی)
  - `src/components/BreadcrumbStructuredData.astro` (پشتیبانی پویا از سلسله‌مراتب چندسطحی)
  - `public/llms.txt` (اصلاح لینک‌ها و افزودن بخش‌های جدید)
  - `public/robots.txt`
* **صفحات اصلی و ایستا:**
  - `src/pages/index.astro` (تایتل، اسکیما و لینک‌های موضوعی)
  - `src/pages/faq/index.astro` (گسترش به ۱۶ سوال، دسته‌بندی و اسکیمای اصلاح‌شده)
  - `src/pages/about/index.astro` (افزودن اسکیمای AboutPage و اصلاح متاتگ‌ها)
  - `src/pages/contact/index.astro` (بهبود اسکیمای تماس)
* **خوشه هوش مصنوعی (اولویت ۱):**
  - `src/pages/services/ai/index.astro` (بازنویسی تایتل، افزودن FAQ، اسکیما، محتوای غنی و آلت تصاویر)
  - `src/pages/services/ai/content-creation/index.astro`
  - `src/pages/services/ai/analysis-strategy/index.astro`
  - `src/pages/services/ai/marketing-engagement/index.astro`
* **خوشه محتوا (اولویت ۲):**
  - `src/pages/services/content/content-calendar/index.astro` (اصلاح ساختار اسکیما FAQ و تطابق با کوئری‌های تقویم محتوا)
  - `src/pages/services/content/text-content/index.astro`
  - `src/pages/services/content/social-media-content/index.astro`
  - `src/pages/services/content/visual-content/index.astro`
  - `src/pages/services/content/translation/index.astro`
* **خوشه سئو (اولویت ۳):**
  - `src/pages/services/seo/index.astro`
  - `src/pages/services/seo/content-authority/index.astro` (تارگت کلمه کلیدی اعتبار صفحه)
  - `src/pages/services/seo/technical-onpage/index.astro`
  - `src/pages/services/seo/local-seo/index.astro`
* **خوشه وب و مارکتینگ:**
  - تمام صفحات `services/marketing/*` و `services/web/*` (اصلاح آلت تکراری `خدمات عصر سئو` به آلت‌های توصیفی منحصربه‌فرد و اصلاح اسکیماهای FAQ)
* **وبلاگ:**
  - `src/lib/blog-data.ts`
  - `src/components/Blog/BlogPost.astro`
  - `src/content/blog/digital-marketing-trends-2024.mdx` (به‌روزرسانی تصاویر و متون)
  - `src/content/blog/google-ads-guide-2024.mdx`
  - `src/content/blog/seo-guide-2024.mdx`

---

## ۶. برنامه راستی‌آزمایی و ارزیابی کیفی (Verification Plan)

### ۶.۱. تست‌های خودکار و سلامت بیلد (Automated Verification)
* **بررسی تایپ‌ها و کامپوننت‌های استرو:**
  ```bash
  npm run type-check # یا astro check
  ```
* **تست بیلد استاتیک بدون خطا:**
  ```bash
  npm run build
  ```
* **بررسی عدم وجود خطای ۴۰۴ یا لینک شکسته با اسکریپت تستی در پوشه dist.**

### ۶.۲. اعتبارسنجی سئو و اسکیما (Manual & Tool Verification)
* **تست ریچ ریزالت گوگل (Google Rich Results Test):** اعتبارسنجی کدهای JSON-LD در صفحات خدمات، مقالات وبلاگ و صفحه سوالات متداول جهت تایید سبز شدن `FAQPage`، `Service`، `BreadcrumbList` و `Article`.
* **بررسی تگ‌های Meta و OpenGraph:** استخراج تمامی `<title>`ها و `<meta description>`های خروجی بیلد و بررسی قرار داشتن در محدوده استاندارد (تایتل بین ۵۰ تا ۶۵ کاراکتر، دیسکریپشن بین ۱۳۰ تا ۱۵۵ کاراکتر).
* **ممیزی تگ آلت تصاویر:** بررسی اینکه هیچ تصویری فاقد `alt` نبوده و هیچ تگ `alt` تکراری غیراستانداردی در پروژه باقی نمانده باشد.
* **بررسی فایل‌های هوش مصنوعی:** تایید دسترسی‌پذیری و صحت محتوایی `https://asreseo.com/llms.txt` و `https://asreseo.com/llms-full.txt`.
